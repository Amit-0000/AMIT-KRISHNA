# Data — never commit raw audio (gigabytes)
/data/
/evaluation/dataset/
/evaluation/dataset_fornorm/

# Model checkpoints — large binary files, mount at runtime
checkpoints/

# Experiment tracking logs (generated artifacts)
runs/
wandb/
training/*.log

# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/
.pytest_cache/

# Jupyter
.ipynb_checkpoints/

# macOS
.DS_Store

# Claude Code internal tooling
.claude/

# Node — dependency tree, never commit; lockfiles/manifests stay tracked
node_modules/

# Database journals / scratch artifacts
*.db-journal
