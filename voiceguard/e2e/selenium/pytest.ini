[pytest]
addopts = -v
markers =
    critical: highest-risk real flows (auth, session guards)
    high: core user-facing flows (registration, scan flow, dashboard)
    medium: secondary flows (history, notifications, navigation)
    low: cosmetic/supporting pages (help, feedback, responsive checks)
