{
  "run_date": "2026-08-04",
  "environment": "local docker-compose (postgres+redis+backend), started for this pass",
  "completed_steps": [
    "setup_env.py",
    "lib/fixtures.py",
    "01_authn_bypass.py",
    "04_rbac_matrix.py",
    "03_idor.py",
    "05_token_tampering.py",
    "06_injection_probe.py",
    "07_rate_limiting.py",
    "08_hardcoded_creds.py",
    "09_generate_report.py"
  ],
  "endpoints_discovered": 39,
  "total_test_records": 223,
  "findings": 1,
  "notes": [
    "Auth is cookie-based (access_token/refresh_token JWT, httponly), not Bearer headers.",
    "No self-serve admin signup path exists; admin test account was promoted via a direct SQL UPDATE against the local docker postgres for RBAC testing only.",
    "No code path in the app ever creates a Notification row, so IDOR could not be tested against a real owned notification (see 03_idor.py note) -- re-test if that feature is wired up later.",
    "One finding: POST /predict (top-level legacy endpoint, no /api/v1 prefix) has zero auth and zero rate limiting -- confirmed 30/30 burst succeeded."
  ]
}
